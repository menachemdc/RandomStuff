<%= form_for([@place, @place.comments.build]) do |f| %>
	<p>
		<%= f.label :bru %><br>
    	<%= f.text_field :commenter %>

	</p>
	<p>
		<%= f.label :remarks %><br>
    	<%= f.text_area :body %>
	</p>
	<p>
    	<%= f.submit "Click here Bru"%>
	</p>
<% end %>
